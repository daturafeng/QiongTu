# QiongTu 项目开发规则

本文件适用于整个仓库。详细且可验收的开发治理规范以 `openspec/specs/development-governance/spec.md` 为唯一事实来源；若该主规格尚未生成，读取活动变更 `openspec/changes/establish-development-governance/`。本文件只提供后续会话必须自动执行的入口规则。若两者不一致，先创建或更新 OpenSpec 变更使其一致，详细主规格优先。

## 会话启动检查

任何软件设计、实现、诊断、测试、发布或流程变更开始前，必须按顺序：

1. 读取本文件与 `openspec/config.yaml`。
2. 确认 OpenSpec CLI 可用并运行 `openspec list`/`openspec status`；若命令不在 PATH，先只读检查 NVM 已安装版本中的 `openspec.cmd`，使用 Node.js 20.19+ 对应版本直接运行，不擅自切换用户的 NVM 当前版本。
3. 读取适用活动变更的 proposal、specs、design、tasks 和任务状态。
4. 检查仓库、依赖、构建/测试命令和工作区现状；保留用户已有改动。
5. 将请求归类为探索、需求更新、实现、诊断、验证、发布或运营，再使用对应 OpenSpec 工作流。

已有变更覆盖请求时必须继续该变更，不得创建平行计划。OpenSpec 不可用或所需工件缺失时，明确报告并先恢复前置条件；恢复前不得进行正式软件设计或实现。

## 唯一事实来源

- OpenSpec 保存需求、验收场景、技术决策、任务、状态和变更历史。
- 不使用 OpenSpec 之外的提案、设计或实施计划作为平行流程；其他文档只能记录操作信息或引用 OpenSpec。
- 需求、范围、兼容性或技术决策变化时，必须先更新所有受影响的 OpenSpec 工件并严格校验，再改代码。
- 实现按 `tasks.md` 的依赖顺序小步推进；只有完成并验证后才能将 `- [ ]` 改为 `- [x]`。

## 生命周期与门禁

默认流程：

`目标与验证 → MVP/提案 → 可验收规格 → 技术设计 → 可执行任务 → 实现 → 系统验证 → 发布准备 → 试运行 → 正式发布 → 运营反馈`

小型变更可以压缩阶段，但不能跳过适用的需求、验证、数据保护与回滚责任。

### Definition of Ready

任务进入实现前必须具备：关联需求与验收场景、已解决的必要设计决策、已知依赖和影响范围、可执行验证方法，以及与风险相称的恢复或回滚方式。任一项不清楚时先更新 OpenSpec，不猜测编码。

### Definition of Done

任务或功能只有在实现完成、自审/评审完成、适用自动测试通过、异常与边界场景处理、验收场景通过、必要安全/性能检查完成、OpenSpec 与操作文档同步，并具备适用部署和回滚方式时才算完成。

### Release Gate

正式版本必须具备可重复构建、版本标识、测试报告、已知限制、兼容矩阵、安装/升级/卸载验证、备份恢复说明、发布步骤和可执行回滚方案。存在高严重度缺陷、数据丢失风险、安全阻断或核心流程失败时禁止发布；系统验证后先受控试运行，再正式发布。

## 请求类型边界

- 探索/分析：可以进行只读研究、样本检查和假设整理，不实施产品代码。
- 需求或设计变化：先更新既有 OpenSpec 变更并保持 proposal/specs/design/tasks 一致。
- 实现：先确认工件完整且严格校验通过，再按 tasks apply。
- 诊断：定位原因并给证据；除非用户同时要求修复，不扩大为实现。
- 发布：执行发布门禁，不以“能运行”代替安装、恢复和回滚验证。
- 运营反馈：把行为变化转化为新的或既有 OpenSpec 变更，不直接改正式实现。

## 审慎建议与用户选择

- 当基于与用户目标相关的具体证据判断请求不合理、风险过高或与已确认 OpenSpec 决策冲突时，可以并应当提出反对意见；说明冲突、取舍、推荐方案及不直接执行的原因。
- 若推荐路径会实质改变成本、风险、架构、数据范围、外部影响或长期维护责任，执行前必须呈现主要选项并取得用户明确选择；不得以技术偏好代替用户决定。
- 对已在用户授权范围内、低风险且不改变已确认范围的常规步骤，应直接执行，不得制造不必要的确认门槛。
- 安全、合规和数据保护硬性约束优先于用户指令；无法执行时说明原因并提供安全替代方案。

## 风险分级验证

每项变更至少执行受影响范围的静态检查、单元、集成或端到端验证中的适用组合。以下领域必须增加专项验证：

- GeoTIFF、点云、OBJ/FBX、3D Tiles、高斯和坐标转换：外部工具/目标运行时回读，检查位置、轴向、尺度、格式和兼容版本。
- 大影像、GPU/CUDA、显存与长任务：固定样本基准、资源峰值、取消、重启、失败恢复和磁盘不足测试。
- 原始数据、正式成果、数据库和安装升级：校验值、备份、恢复、卸载不删用户数据和版本回滚测试。
- 本地 HTTP 与外部输入：路径越界、参数注入、回环绑定、访问控制和日志脱敏检查。

不能运行的验证必须说明原因、风险与替代证据，不得把未验证任务标记完成。

## Git 条件门禁

当前若不是 Git 仓库，必须如实报告，不能声称已执行分支、提交或 CI 检查，也不得未经用户授权擅自初始化 Git。Git 可用后，源代码、OpenSpec、配置和迁移纳入版本控制；提交保持小而清晰，提交信息使用中文，禁止提交密钥、原始航拍数据、大型派生成果和构建缓存，合并前运行适用验证。

## 总控式并行开发

- 多个 Codex 窗口并行时，指定一个总控窗口维护 OpenSpec 依赖、任务边界、写入所有权、集成顺序、验证和最终状态；执行窗口不得自行扩大范围或建立竞争性设计。
- 每个并行任务必须明确任务编号、输入、允许修改路径、验收命令、禁止事项和阻塞升级条件。研究结论只有在总控复核并同步 OpenSpec 后才能成为项目决策。
- 写入型任务优先使用从同一已验证基线创建的独立 Git worktree，并以小提交交回总控。无法安全建立 worktree、存在未共享变更或修改路径重叠时，共享工作区只允许一个写入者，其余窗口只读研究或等待。
- 总控集成并在目标基线上复验后才更新 OpenSpec 任务状态；执行窗口不得仅凭自己的分支结果宣称跨任务完成。
- 当前活动总控窗口默认使用 GPT-5.6；由总控按需创建的新执行窗口默认使用 GPT-5.5。只有任务不依赖同一未决决策、写入路径不重叠且可独立验收时才创建执行窗口，不以窗口数量代替实际进度。
- 只有指定模型在当前 Codex 模型目录中可用且运行时允许模型覆盖时，才按上述模型路由创建窗口。模型不可用、目录在 Codex 启动后变化、运行时要求重启或创建失败时，不静默改用其他模型、不无变化重试；总控可串行推进安全工作，并记录重启 Codex 或由用户解除的条件。
- 模型选择只是开发执行配置，不改变 OpenSpec、安全、数据、工具权限与验收门禁。

### 阻塞与依赖安装

1. 记录首个相关错误和复现条件，不用重复输出掩盖根因。
2. 若来源可信、版本固定、许可证已识别、项目作用域且可回退的推荐依赖能解决阻塞，可在已授权任务内安装，记录版本，并重新验证。
3. 同一失败命令或假设在环境、输入、配置和证据没有变化时不得重复；同一阻塞最多尝试两个相互独立且风险相称的安全修复方向。
4. 两种修复仍失败时停止该分支并交回总控。涉及系统驱动或重启、全局环境切换、账号/付费、产品架构、数据外部影响、再分发义务或不可逆操作时，不消耗重试额度，直接由总控按审慎建议门禁处理；总控在现有授权内仍不能解决时再请求用户决定。
5. 安装开发依赖不能替代根因分析，也不能让开发期容器、WSL 或工具自动成为最终产品运行依赖。

## 数据与环境保护

- 默认只读处理仓库外的无人机原始影像；未经用户明确要求不得移动、改写或删除。
- 密钥、令牌、许可证和机器专属路径不得写入提交内容；使用示例或环境变量说明。
- 不覆盖用户现有环境、NVM、JDK、IDE 或运行配置。Java 后端启动继续遵循用户级 IDEA MCP 前置规则。

## 会话交接

结束软件工作时必须报告：活动变更、完成的任务、实际执行的验证、未完成任务、阻塞与已知风险、下一安全步骤。确保 OpenSpec 任务状态真实，使后续会话仅依靠本文件与 OpenSpec 即可继续。
