## 1. 安全迁移项目根目录

- [x] 1.1 解析并验证当前工作区父目录与目标 `QiongTu` 的绝对路径，确认目标位于父目录内、尚不存在，且迁移白名单只包含 `AGENTS.md`、`.codex`、`openspec`。
- [x] 1.2 创建 `QiongTu` 并逐项移动白名单工件；检查源位置不再有活动副本、目标内容完整，并确认未访问或改变用户指定的仓库外参考数据。

## 2. 建立公开仓库内容边界

- [x] 2.1 创建 `.gitignore`，覆盖密钥/环境、IDE/系统、日志/临时、依赖/构建以及输入/输出/工作区/检查点/模型目录，并用代表性路径验证忽略规则。
- [x] 2.2 创建真实描述当前阶段的 `README.md`，链接活动产品定义变更和开发治理主规格，不复制需求或宣称尚未实现的能力可用。
- [x] 2.3 更新 OpenSpec 项目上下文中的仓库信息，不写入机器专属路径、凭据或许可证，并执行变更级严格校验。

## 3. 初始化本地 Git 仓库

- [x] 3.1 在 `QiongTu` 中以 `main` 初始化 Git，配置 `origin` 为用户指定的 HTTPS URL，并验证分支、远端以及未修改全局 Git/NVM 配置。
- [x] 3.2 审核所有待提交文件、文件大小和忽略结果，确认不存在密钥、仓库外影像、依赖、构建缓存或大型处理成果；执行 OpenSpec 全量严格校验和 Git 暂存差异检查。

## 4. 发布并验证初始仓库

- [ ] 4.1 使用现有 Git 身份创建范围清晰的初始提交，推送 `main` 并设置跟踪 `origin/main`；认证或网络失败时保留本地提交并报告错误。
- [ ] 4.2 查询远端 `main` 引用并核对本地跟踪状态，确认首次发布成功后再进入完成整理。

## 5. 完成 OpenSpec 生命周期

- [ ] 5.1 将 `repository-bootstrap` 增量规格同步为主规格，复核需求与场景语义完整一致。
- [ ] 5.2 执行 OpenSpec 全量严格校验，确认所有验收证据齐全且本变更已具备归档条件；随后按归档流程记录完成状态并将该记录推送到 `origin/main`。
